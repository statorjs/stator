node_modules/
dist/
*.vsix
.DS_Store
*.log
.env
.env.local
coverage/
.vitest-cache/

# Local Netlify folder
.netlify

# Chisel generated cache / indices (root and any nested docs source)
**/.chisel/index.db
**/.chisel/update_check.json

# Private apps live on feature branches, not on main. Branches that want
# to track files under apps/private/ remove this line.
apps/private/
dump.rdb
cf-chain.pem

# Private working notes (backed up outside source control)
.private/

# Claude Code local session state (agent worktrees, per-machine settings).
# A shared .claude/settings.json, if one ever exists, stays committable.
.claude/worktrees/
.claude/settings.local.json
