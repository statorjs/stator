---
"stator-vscode": minor
---

The extension's bundled compiler now accepts `on:` directives on component tags (directive forwarding) instead of raising a "not valid on a component" error, and types the `Stator.forwarded()` accessor.
