---
"stator-vscode": minor
---

The bundled compiler follows Stator 2.0: `bind:` is a located error with migration guidance, client-machine `read()` is the display path, and the one-bag `machine()` form is gone.
