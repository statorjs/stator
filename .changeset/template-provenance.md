---
"create-stator": minor
---

Scaffolding records template provenance in package.json — the source, ref, and resolved commit under a `stator.template` key — so template updates have a merge base. See the new "Updating from a template" recipe in the docs.
