---
"create-stator": patch
---

Scaffolded apps now declare `@statorjs/stator ^1.6.0` (the range had trailed at `^1.4.0` since 1.5); a CI check keeps the scaffold range in step with the framework version from now on.
