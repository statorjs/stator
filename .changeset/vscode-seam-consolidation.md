---
"stator-vscode": patch
---

The bundled compiler shares the component props-type computation with the `.d.ts` generator (one implementation, seam-tested), and inherits the consolidated attribute/text value contracts.
