---
"@statorjs/stator": patch
---

Per-element JSX types learn the attributes real forms and images need: `enctype` on `form`, `accept`/`multiple` on `input`, and `srcset`/`sizes`/`decoding`/`fetchpriority` on `img`. Found the same way `<time datetime>` was — the first app to build an upload form and photo markup hit each as a compile error.
