---
"stator-vscode": minor
---

The extension's bundled compiler now supports `{...rest}` attribute spread in templates — `<button {...rest}>` and `<Card {...rest} />` compile instead of raising a spread-not-supported error.
